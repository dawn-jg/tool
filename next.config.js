/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: 'export', // removed: incompatible with i18n client components
  images: {
    unoptimized: true,
  },
};

module.exports = nextConfig;
